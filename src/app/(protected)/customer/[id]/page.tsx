import React from "react";
import CustomerId from "@/features/customer/component/CustomerId";

const page = () => {
  return (
    <div className="py-4">
      <CustomerId />
    </div>
  );
};

export default page;
